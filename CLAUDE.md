# codex-tutorial-kr

비전공 학부생을 대상으로 한 ChatGPT Codex 한국어 튜토리얼 사이트.
Astro Starlight 기반, GitHub Pages로 공개 운영.

앞으로 이 저장소에서 작업할 때 아래 규칙을 항상 지킬 것.

---

## 독자 전제

- 독자는 **터미널, Git, 레포지토리, 커밋, 의존성, 배포**를 모른다고 가정한다.
- 독자는 자기가 제대로 했는지 스스로 판단하지 못한다. 매 단계 성공 확인 방법을 준다.
- 독자는 영어 문서를 읽지 않는다. 영어 UI 명칭은 병기하되 설명은 한국어로 한다.

---

## 사실 관계 규칙 (최우선)

Codex는 변화가 매우 빠른 제품이다. 다음은 **절대 추측해서 쓰지 않는다.**

- 버전 번호, 모델명
- 요금제, 사용 한도, 무료 제공 범위
- 화면 구성, 메뉴 이름, 버튼 위치
- 설치 명령어의 정확한 문법
- 외부 URL

확실하지 않으면 본문에 아래 형식으로 남긴다.

```
<!-- TODO:확인 — 무엇을 확인해야 하는지 구체적으로 -->
```

스크린샷 자리는 이미지 태그 대신 아래로 표시한다.

```
<!-- IMG: 어떤 화면을 어떤 상태로 캡처할지 설명 -->
```

**존재를 확인하지 못한 URL은 링크로 만들지 않는다.** 링크가 필요하면 TODO로 남긴다.

작업이 끝날 때마다 그 작업에서 새로 생긴 `TODO:확인`과 `IMG` 마커를 목록으로 보고한다.

---

## 문서 작성 규칙

### 구조

- 모든 실습 페이지는 `src/content/docs/_template.md` 구조를 따른다.
- 페이지 하나가 화면 3스크롤을 넘길 것 같으면 작성 전에 분할을 제안한다.
- 각 페이지 frontmatter에 검증일과 대상 버전을 넣는다.

### 문체

- `~합니다`체. 간결하게.
- 감탄사, 격려 문구, 이모지를 쓰지 않는다.
- **금지 표현**: "적절히", "알아서", "필요에 따라", "간단히", "쉽게", "그냥"
  → 입문자에게는 구체적 지시만 준다.

### 용어

- 전문 용어가 페이지에서 처음 등장하면 `/reference/glossary` 로 링크한다.
- 용어집에 없는 용어를 링크하려면 먼저 용어집에 항목을 추가한다.

### 프롬프트 예시

- Codex에게 보낼 프롬프트 예시는 **복붙 가능한 완결된 문장**으로 코드블록에 넣는다.
- "이런 식으로 요청하세요" 같은 요약 설명으로 대체하지 않는다.

### 실패 처리

- 실패 시나리오는 부록으로 빼지 않고 해당 단계 바로 아래에 둔다.
- 각 실습 페이지에 자주 나는 에러 최소 3개를 증상 → 원인 → 해결 순으로 쓴다.

---

## 창구 서술 순서

Codex의 5가지 사용 창구는 **항상 입문자 기준 난이도 순**으로 다룬다.

1. Codex 앱 (기본 경로)
2. IDE 확장
3. 웹
4. CLI
5. GitHub 연동

앱을 메인 경로로 쓰고, 나머지는 `/surfaces` 섹션에 모은다.
초반 페이지에서 5개를 동시에 나열해 독자를 혼란스럽게 하지 않는다.

---

## 기술 스택

- Astro Starlight, 한국어 단일 언어
- 검색: Pagefind (Starlight 기본)
- 배포: Vercel (GitHub 저장소 연결 시 자동 배포)
- 콘텐츠는 전부 Markdown (`src/content/docs/`)

---

## 작업 방식

- 한 번에 한 섹션씩 작업한다. 여러 섹션을 동시에 채우지 않는다.
- 점검 요청을 받으면 **고치기 전에 문제 목록을 먼저 보고**한다.
- 커밋 메시지는 한국어로, `섹션: 변경 내용` 형식.

---

## 개발 환경 참고

개발 서버를 시작할 때는 백그라운드 모드를 사용한다.

```
astro dev --background
```

`astro dev stop`, `astro dev status`, `astro dev logs` 로 백그라운드 서버를 관리한다.

전체 문서: https://docs.astro.build
